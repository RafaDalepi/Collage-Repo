// vetor de tamanho N com N numeros primos
// começa com primeiro numero primo conhecido = 2
// divide pelos ultimos numeros primos conhecidos
// aumentando de 1 em 1

#include <stdio.h>
#include <stdlib.h>

int calculaPrimos(int num){
    int i, *numPrimo[num];
    for (i = 0; i < num; i++){
        
    }
    
}

int main(){
    int numeros;

    printf("Quantidade de numeros primos: ");
    scanf("%d", &numeros);

    calculaPrimos(numeros);
}